"use client";

import QuizFlow from "@/components/quiz/QuizFlow";

/**
 * /quiz/discover — Three-stage quiz experience.
 * All stage state and API calls live inside <QuizFlow />.
 */
export default function QuizDiscoverPage() {
  return <QuizFlow />;
}
